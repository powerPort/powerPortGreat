var app = require('../index.js');

//use body parser to read body content in the requests
app.use(require('body-parser'));

app.get('/' , (req, res) => {
 //do some thing
 res.send('the routes are on');
});

